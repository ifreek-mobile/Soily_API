[pytest]
pythonpath = .
markers =
    unit: pure function unit tests
    integration: api-level integration tests
    e2e: live server tests
filterwarnings =
    ignore:coroutine 'Semaphore.acquire' was never awaited:RuntimeWarning